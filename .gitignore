# Python-Bytecode
__pycache__/

# Persönliche Laufzeitdaten (werden vom Panel geschrieben)
zahnrad_werte.json
zahnrad_cache.json

# Fremd-/Temporärdateien (nicht Teil dieses Repos)
kabelclip/
*.backup

# Build-Abhängigkeiten des Online-Generators (nur zum Bauen, nicht deployen)
node_modules/
package-lock.json

# Generierte Bundles: werden im Pages-Workflow aus web/js/*.js gebaut,
# nicht committet (siehe .github/workflows/pages.yml).
web/js/shell.bundle.js
web/js/viewer.bundle.js

# Android-Build
android/build/
android/app/build/
android/.gradle/
android/local.properties
# Die Web-App wird beim Bauen aus web/ hierher kopiert, nicht gepflegt.
android/app/src/main/assets/
# Signaturschluessel gehoert NICHT ins Repo (siehe APK-HERUNTERLADEN.md)
*.jks
*.keystore
android/keystore.properties
